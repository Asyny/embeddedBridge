#include "DataHandling/DataModel.h"

namespace DataModel
{
    struct ainSettings ain;
    struct relaisSettings relais;
    struct vccSettings vcc;
    struct vccSettings vdc;
    struct doutSettings dout1;
    struct doutSettings dout2;
    struct doutSettings dout3;
    struct doutSettings dout4;
    struct foutSettings fout;
    struct batSettings bat;
    struct lshiftSettings lshift;
};